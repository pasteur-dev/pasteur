tables:
  table:
    primary_key: id

    fields:
      AGEP: numerical
      SCHL: ordinal
      MAR: categorical
      SEX: categorical
      DIS: categorical
      ESP: categorical
      MIG: categorical
      RELP: categorical
      RAC1P: categorical
      PUMA:
        type: numerical    # ~270 (per state) PUMA codes; PrivPGD bins to 32
        bins: 32
      ST: categorical
      CIT: categorical
      OCCP:
        type: numerical    # ~530 occupation codes; PrivPGD bins to 32
        bins: 32
      JWTR: categorical
      POWPUMA:
        type: numerical    # ~270 work-PUMA codes; PrivPGD bins to 32
        bins: 32
      POVPIP: numerical
      label: categorical

ratios:
  wrk: 0.8
  ref: 0.2
