default:
  ratios:
    wrk: 0.8
    ref: 0.2

  algs:
    privbayes:
      e1: 0.3
      e2: 0.7
      theta: 4

    aim:
      dir: ${private_pgm}
      venv: ${private_pgm}/venv
      e: 1

    mrf:
      dir: ${privmrf}
      venv: ${privmrf}/venv
      e: 1.120
